{
  "version": "0.8.0-alpha.3",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "dev": "vitepress dev",
    "build": "vitepress build",
    "serve": "vitepress serve",
    "docs:deploy": "npm run build && sh ./deploy.sh",
    "docker-build": "docker build -t lafyun/docs:0.8.0-alpha.3 ."
  },
  "dependencies": {
    "vitepress": "^0.22.4"
  }
}
